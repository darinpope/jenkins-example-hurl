pipeline {
  agent any
  stages {
    stage('Verify Hurl installation') {
      steps {
        sh 'hurl --version'
      }
    }
    stage('') {
      steps {
        dir('example-1') {
          sh 'hurl --no-output basic.hurl'
        }
      }
    }
  }
}